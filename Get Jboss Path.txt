String projectPath = "Sorry, unable to get file path right now!";
try {
	File directory = new File(".");
	projectPath = directory.getCanonicalPath().concat(File.separatorChar+"cetmsDocs");
} catch (IOException e) {
} 
System.out.println("JBoss path that we got : " + projectPath);

String replaceString = projectPath.replace('\\','/');
replaceString = replaceString.replace(":/","://");
replaceString = replaceString + "/";
System.out.println("JBoss path trying to change : " + replaceString);

String fileSavePath = "C://Programming/Folder/Folder_Photos/";
System.out.println("JBoss path should look like this : " + fileSavePath);