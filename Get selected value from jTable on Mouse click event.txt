Point point = evt.getPoint();
        int row,column;
        row  = table_studentdetails.rowAtPoint(point);	//table_studentdetails - This is name of the table
        column  = table_studentdetails.columnAtPoint(point);
      
        System.out.println(row);
        System.out.println(column);
        
        
        Long cell_value = (Long)(table_studentdetails.getValueAt(row, column)); // Column can be zero if you want the user to select only the primary key (Most Cases)
        
// Enter cell_value directly in SQL Query.
 
ResultSet rs=st5.executeQuery("SELECT student_id, roll_no, std_surname, std_name, class FROM student_master WHERE student_id = "+cell_value+"");